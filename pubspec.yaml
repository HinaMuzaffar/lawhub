name: lawhub
description: A Flutter app for law hub

version: 1.0.0+1

environment:
  sdk: '^3.7.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  logger: ^2.5.0
  mockito: ^5.4.0 

flutter:
  uses-material-design: true
firebase_core: ^2.25.4
firebase_auth: ^4.17.6
http: ^0.13.6
